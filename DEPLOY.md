# راهنمای استقرار و استفاده از Aether

این راهنما مراحل اجرای سرور روی لینوکس و اتصال کلاینت از مک را توضیح می‌دهد.

## پیش‌نیازها (Requirements)

### برای سرور (Server)
*   **سیستم عامل:** لینوکس (Ubuntu, Debian, CentOS, و...)
*   **پورت:** پورت **4242** باید باز باشد.

---

## ۱. نصب خودکار روی سرور (روش جدید و آسان) 🚀

ما یک اسکریپت مدیریت (`manage.sh`) آماده کردیم که همه کارها را انجام می‌دهد.

1.  **آپلود فایل‌ها:**
    فایل باینری سرور و اسکریپت مدیریت را به سرور منتقل کنید:
    ```bash
    # تغییر نام فایل باینری به نام استاندارد
    mv bin/server-linux-amd64 aether-server
    
    # آپلود به سرور
    scp aether-server scripts/manage.sh USER@SERVER_IP:~
    ```

2.  **نصب و اجرا:**
    در سرور دستورات زیر را بزنید:
    ```bash
    chmod +x manage.sh
    sudo ./manage.sh install
    ```
    
    پس از نصب، اسکریپت یک **JSON** به شما می‌دهد. آن را کپی کنید؛ این کانفیگ کلاینت شماست.

3.  **مدیریت سرور:**
    *   دیدن لاگ‌ها: `sudo ./manage.sh logs`
    *   استاپ کردن: `sudo ./manage.sh stop`
    *   حذف کامل: `sudo ./manage.sh uninstall`

---

## ۲. راه‌اندازی کلاینت (مک/ویندوز)

1.  کانفیگ JSON که از مرحله قبل گرفتید را در فایل `config.json` کنار برنامه کلاینت ذخیره کنید.
2.  کلاینت را اجرا کنید.
3.  تلگرام یا مرورگر را روی `127.0.0.1:1080` تنظیم کنید.
