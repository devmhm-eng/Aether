# راهنمای تغییر آی‌پی (Proxy Settings)

تبریک! VPN شما (`Aether`) کاملاً سالم است و به سرور وصل شده است.
دلیل اینکه آی‌پی شما در مرورگر عوض نشده، این است که **باید به سیستم بگویید از این تونل استفاده کند.**

## روش ۱: تنظیم کل سیستم (macOS System-wide)

این روش کل ترافیک مک (تلگرام، سافاری، کروم و...) را از فیلترشکن رد می‌کند.

1.  به مسیر زیر بروید:
    `System Settings` -> `Network` -> `Wi-Fi` (یا Ethernet) -> `Details...`
2.  از سمت چپ گزینه **Proxies** را انتخاب کنید.
3.  گزینه **SOCKS Proxy** را روشن کنید.
4.  اطلاعات زیر را وارد کنید:
    *   **Server:** `127.0.0.1`
    *   **Port:** `1080`
    *   (گزینه User/Password را خالی بگذارید)
5.  دکمه **OK** را بزنید.

حالا دوباره سایت `whatismyip.com` را چک کنید. آی‌پی شما باید `46.38.254.232` باشد. 🇳🇱

---

## روش ۲: فقط مرورگر (Chrome/Edge Extension)

اگر می‌خواهید فقط مرورگر وصل شود (و بقیه سیستم عادی بماند):

1.  افزونه **SwitchyOmega** (یا مشابه) را نصب کنید.
2.  یک پروفایل جدید بسازید.
3.  Protocol را روی **SOCKS5** بگذارید.
4.  Server: `127.0.0.1` و Port: `1080`.
5.  تیک بزنید (Apply Changes) و فعال کنید.

---

## روش ۳: تلگرام (Telegram)

1.  به `Settings` -> `Data and Storage` -> `Proxy Settings` بروید.
2.  `Add Proxy` را بزنید.
3.  نوع را **SOCKS5** انتخاب کنید.
4.  Server: `127.0.0.1` و Port: `1080`.
