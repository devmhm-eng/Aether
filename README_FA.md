# هسته اِتِر (Aether Core) 🌌

هسته اِتِر (Aether Core) یک موتور عبور از فیلترینگ پیشرفته و چندلایه است که با زبان Go نوشته شده و برای مقابله با سیستم‌های DPI (بازرسی عمیق بسته) طراحی شده است. این هسته به جای استفاده از یک پروتکل ثابت، از ترکیب چندین تکنیک برای استتار ترافیک استفاده می‌کند.

## قابلیت‌های کلیدی (Core Features)

### ۱. موتور فلاکس (Flux Engine) - حمل‌نقل ترکیبی 🚀
این موتور هوشمند به طور خودکار بهترین بستر انتقال داده را انتخاب می‌کند و در صورت مسدود شدن یک روش، بدون قطعی به روش دیگر سوییچ می‌کند:
*   **TCP:** برای ارتباطات پایدار.
*   **WebSocket:** برای مخفی شدن پشت CDN ها (مثل Cloudflare).
*   **WebRTC:** استفاده از کانال‌های دیتای WebRTC برای ایجاد تونل‌های نامرئی که شبیه تماس صوتی/تصویری به نظر می‌رسند.
*   **QUIC/UDP:** برای سرعت بالا و کاهش تأخیر.

### ۲. پروتکل شبح (Ghost Protocol) - دست دادن مخفی 👻
قبل از برقراری هرگونه اتصال VPN، یک "دست دادن" (Handshake) جعلی انجام می‌شود.
*   کلاینت ابتدا یک درخواست HTTP معمولی (مثل آپلود فایل یا دیدن یک عکس) می‌فرستد.
*   اگر فایروال این بسته را بررسی کند، آن را یک ترافیک وب سالم و معمولی می‌بیند.
*   ترافیک واقعی VPN تنها **پس از** تایید این مرحله و در دل این ارتباط مخفی می‌شود.

### ۳. لایه سراب (Mirage Layer) - مبدل ترافیک 🏜️
این لایه وظیفه دارد شکل و شمایل بسته‌ها را تغییر دهد تا الگوی مشخصی نداشته باشند.
*   **رمزنگاری:** استفاده از ChaCha20Poly1305 برای امنیت فوق‌العاده بالا.
*   **تغییر سایز پویا:** اضافه کردن داده‌های تصادفی (Padding) به بسته‌ها تا تحلیل‌گرهای ترافیک نتوانند از روی سایز بسته، محتوا را حدس بزنند.

### ۴. سایرن (Project Siren) - تقلید VoIP 🧜‍♀️
این ماژول مخصوص ترافیک UDP است.
*   ترافیک VPN را در قالب بسته‌های RTP (پروتکل انتقال صدا در تماس‌های اینترنتی مثل واتس‌اپ یا تلگرام) بسته‌بندی می‌کند.
*   فیلترینگ که معمولاً روی تماس‌های صوتی حساس نیست، اجازه عبور این بسته‌ها را می‌دهد.

### ۵. اثر انگشت مرورگر (uTLS) 🦊
برای جلوگیری از شناسایی شدن کلاینت به عنوان یک "VPN"، هسته اِتِر رفتار دقیق مرورگرهای واقعی (مثل Chrome یا Firefox) را در لایه TLS تقلید می‌کند. این باعث می‌شود سرور فیلترینگ تصور کند با یک کاربر عادی که در حال وب‌گردی است روبرو است.

### ۶. دفاع فعال (Smart Honeypot) 🍯
اگر کسی (مثل سانسورچی یا ربات‌های اسکنر) سعی کند بدون کلید معتبر به سرور متصل شود:
*   سرور اتصال را قطع نمی‌کند (که مشکوک باشد).
*   در عوض، ترافیک را به یک وب‌سایت استاتیک و معمولی (مثل یک سایت خبری یا فروشگاهی) هدایت می‌کند.
*   این باعث می‌شود IP سرور لو نرود و امن باقی بماند.

---
**وضعیت پروژه:** این هسته هم‌اکنون پایدار بوده و آماده استفاده به عنوان Backend برای اپلیکیشن‌های اندروید، iOS و دسکتاپ است.
