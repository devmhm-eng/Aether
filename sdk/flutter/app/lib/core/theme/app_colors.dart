import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFFAD7AF1); // Purple
  static const Color secondary = Color(0xFF413D46); // Dark Grey
  static const Color background = Color(0xFF000000); // Black

  static const Color topGradient = Color(0xFF000000);

  // Gradient Colors (Ready To Connect)
  static const Color topGradientReadyToConnect = Color(0xFF000000);
  static const Color middleGradient = Color(0xFF413D46);
  static const Color bottomGradient = Color(0xFF2C2C2C);

  // Gradient Colors (Connected)
  static const Color middleGradientConnected = Color(0xFF00C853); // Green
  static const Color bottomGradientConnected = Color(0xFF1B5E20); // Dark Green

  // Gradient Colors (No Internet)
  static const Color middleGradientNoInternet = Color(0xFF9A2635);
  static const Color bottomGradientNoInternet = Color(0xFFE72E44);

  // Gradient Colors (Failed To Connect)
  static const Color middleGradientFailedToConnect = Color(0xFF867229);
  static const Color bottomGradientFailedToConnect = Color(0xFFD9B639);

  // Connecting Colors
  static const Color topGradientConnecting = Color(0xFF000000);
  static const Color middleGradientConnecting = Color(0xFF6B4BA3); // Deep Purple
  static const Color bottomGradientConnecting = Color(0xFFAD7AF1); // Purple

  // Download, Upload and Warning Colors
  static const Color warningColor = Color(0xFFFFAA66);
  static const Color downloadColor = Color(0xFF76F959);
  static const Color uploadColor = Color(0xFF72D9FF);
}
